import 'package:flutter/material.dart';

/*class DailyWordle extends StatelessWidget {
  const DailyWordle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('item1'),
    );
  }
}*/

/*class PracticeWordle extends StatelessWidget {
  const PracticeWordle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text('Item2'),);
  }
}*/

/*class MultiplayerWordle extends StatelessWidget {
  const MultiplayerWordle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Item2'),
    );
  }
}*/
