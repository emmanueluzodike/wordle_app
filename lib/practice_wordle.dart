import 'package:flutter/material.dart';

class PracticeWordle extends StatelessWidget {
  const PracticeWordle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Practice Wordle'),
    );
  }
}
